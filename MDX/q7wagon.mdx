/* Relacja pomiêdzy wiekiem taboru i kosztem awarii (WAGONY) */

SELECT
	{[Measures].[Koszt Naprawy SUM]} ON COLUMNS,
	NON EMPTY {[WAGON].[Wiek Kategoria].Children} ON ROWS
FROM AWARIE_WAGON
WHERE [Data zg³oszenia].[DateHierarchy].[2022].[12]
